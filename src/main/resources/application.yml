spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./db/safariguide.db
    username: mh
    password: mh
    initialization-mode: always

  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate.ddl-auto: create-drop

  h2:
    console:
      enabled: true

